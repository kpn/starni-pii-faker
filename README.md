# starni-pii-faker
Given a text containing tagged PII, it replace their values according to their tag, using the faker python package (https://faker.readthedocs.io/en/master/). Currently for Dutch, but very easily extendable to other languages.
